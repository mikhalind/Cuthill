<Window x:Class="Cuthill.RankWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cuthill"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d" ResizeMode="NoResize"
        Title="Ввод значений" Height="125" Width="250">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Размерность:"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   FontSize="16" Margin="10,10,5,5"/>
        <TextBox x:Name="InputBox" TextChanged="InputBox_TextChanged" FontSize="16" Grid.Row="0" Grid.Column="1" Margin="5,10,10,5"/>
        <Button x:Name="ButtonOK" Content="OK" FontSize="14" Margin="10,5,5,10" Grid.Row="1" Grid.Column="0" Click="ButtonOK_Click"/>
        <Button x:Name="ButtonCancel" Content="Отмена" FontSize="14" Margin="5,5,10,10" Grid.Row="1" Grid.Column="1" Click="ButtonCancel_Click"/>
    </Grid>
</Window>
